import { TimeDifferenceDirective } from './time-difference.directive';

describe('TimeDifferenceDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeDifferenceDirective();
    expect(directive).toBeTruthy();
  });
});
