import { TimeFormatDirective } from './time-format.directive';

describe('TimeFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeFormatDirective();
    expect(directive).toBeTruthy();
  });
});
